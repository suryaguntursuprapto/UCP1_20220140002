<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Kolam Renang</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
        }

        h1 {
            color: #333;
        }

        form input {
            padding: 10px;
            margin: 5px;
            width: 100%;
            box-sizing: border-box;
        }

        button {
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }
    </style>
</head>

<body>
    <h1>Edit Kolam Renang</h1>
    <form action="/pools/edit/<%= pool.id %>" method="POST">
        <input type="text" name="name" value="<%= pool.name %>" required />
        <input type="text" name="location" value="<%= pool.location %>" required />
        <input type="number" name="price" value="<%= pool.price %>" required />
        <button type="submit">Update Kolam</button>
    </form>
</body>

</html>
